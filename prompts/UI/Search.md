
# UI Prompt
Use React and TypeScript for the UI updates.
We will use AWS SDK for S3 operations.
Follow all standards for React and TypeScript.
Validate linting and formatting.
Ask me about anything that you are not sure of.

## ImageList.tsx Component
Update ImageList.tsx component to allow a search at the top of the page that will search the inventory table's user_metadata field of the S3 object using the key of `description`.
